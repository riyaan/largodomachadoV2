apply plugin: 'java'

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile project(':photoshotlist.domainmodels')
    compile project(':photoshotlist.infrastructure')
}

sourceCompatibility = "1.7"
targetCompatibility = "1.7"
